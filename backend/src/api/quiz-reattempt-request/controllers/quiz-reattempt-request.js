'use strict';

/**
 * quiz-reattempt-request controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::quiz-reattempt-request.quiz-reattempt-request');
