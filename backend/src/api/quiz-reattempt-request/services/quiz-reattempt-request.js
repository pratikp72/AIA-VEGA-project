'use strict';

/**
 * quiz-reattempt-request service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::quiz-reattempt-request.quiz-reattempt-request');
