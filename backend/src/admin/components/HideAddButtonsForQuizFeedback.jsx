/**
 * Previously hid "Add an entry" for quiz/feedback in Course edit view.
 * Disabled: feedback_question (and quiz) "Add an entry" buttons are left visible.
 * Component kept so it can be re-enabled later if needed.
 */

function HideAddButtonsForQuizFeedback() {
  return null;
}

export default HideAddButtonsForQuizFeedback;
